/** Watchdog timeout uniform para todos os agentes (minutos). */
export const WATCHDOG_TIMEOUT_MIN = 40

/** Expoente da curva ease-in da ProgressBar (t^power). */
export const PROGRESS_CURVE_POWER = 2.5
